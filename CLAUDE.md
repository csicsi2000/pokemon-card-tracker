See AGENTS.md for how this repo works, how to read and edit the user's collection with the
`cardex` CLI or MCP server, and what to run before finishing (`npm test`, `npm run check`).
For deck-building advice follow docs/deck-coach-prompt.md.
